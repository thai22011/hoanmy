export interface NewsPreview {
    image: string,
    title: string,
    description: string
    }

export interface ProjectPreview {
    image: string,
    title: string,
    category: string
}
    
    export interface Project {
    image: string,
    title: string,
    client: string,
    budget: string,
    
    }