export interface NewsPreview {
    image: string,
    title: string,
    description: string
    slug: string
    }

export interface ProjectPreview {
    image: string,
    name: string,
    category: string
    slug: string
}
    
    export interface Project {
    image: string,
    title: string,
    client: string,
    budget: string,
    
    }