export interface NewsPreview {
    image: string,
    title: string,
    description: string
    slug: string
    }

export interface ProjectPreview {
    image: string,
    name: string,
    market: string
    slug: string
    client: string
}
    
    export interface Project {
    image: string,
    title: string,
    client: string,
    budget: string,
    
    }